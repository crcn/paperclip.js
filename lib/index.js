// Generated by CoffeeScript 1.6.2
(function() {
  var Clip, Parser, clip, evaluator, expr, p;

  Parser = require("./parser");

  Clip = require("./clip");

  p = new Parser();

  expr = p.parse("text: person.age > 5");

  clip = new Clip();

  clip.data.set("name", "craig");

  evaluator = expr.evaluate(clip);

  clip.data.set("person.age", 5);

  clip.data.set("person.age", 6);

  evaluator.actions[0].bind(function() {});

  setTimeout((function() {}), 1000 * 60);

}).call(this);
