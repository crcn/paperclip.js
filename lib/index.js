// Generated by CoffeeScript 1.6.2
(function() {
  var Clip, clip, compile, scripts;

  compile = require("./compile");

  Clip = require("./clip");

  scripts = compile("text: person.name ? 'hey ' + person.name : 'no person available' | uppercase() | swearize(); name: person.name ");

  clip = new Clip({
    data: {
      person: {
        name: "craig"
      }
    },
    script: scripts.text,
    modifiers: {
      uppercase: function(value) {
        return value.toUpperCase();
      },
      swearize: function(value) {
        return "SHIT... " + value;
      }
    }
  });

  clip.data.set("person.name", "craig");

  clip.on("change", function(value) {
    return console.log(value);
  });

  console.log(clip.value);

  clip.data.set("person.name", void 0);

  clip.data.set("person.name", "josh");

}).call(this);
