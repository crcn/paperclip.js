// Generated by CoffeeScript 1.6.2
var Element, StringStream, Text, bindable;

bindable = require("bindable");

Element = (function() {
  /*
  */
  function Element(name) {
    this.name = name;
    this.attributes = {};
    this.childNodes = [];
  }

  /*
  */


  Element.prototype.setAttribute = function(name, value) {
    return this.attributes[name] = value;
  };

  /*
  */


  Element.prototype.appendChild = function(node) {
    return this.childNodes.push(node);
  };

  /*
  */


  Element.prototype.toString = function() {
    var buffer, name;

    buffer = ["<", this.name];
    for (name in this.attributes) {
      buffer.push(name + "=" + attributes[name]);
    }
    buffer.push(">");
    buffer.push.apply(buffer, this.childNodes);
    buffer.push("<", this.name, "/>");
    return buffer.join("");
  };

  return Element;

})();

Text = (function() {
  /*
  */
  function Text(value) {
    this.value = value;
  }

  /*
  */


  Text.prototype.toString = function() {
    return this.value;
  };

  return Text;

})();

StringStream = (function() {
  /*
  */
  function StringStream(context) {
    this.context = context;
    this.internal = new bindable.Object();
  }

  /*
  */


  StringStream.prototype.createElement = function(name) {
    return new Element(name);
  };

  /*
  */


  StringStream.prototype.createTextNode = function(text) {
    return new Text(text);
  };

  return StringStream;

})();

module.exports = StringStream;
