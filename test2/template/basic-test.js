var expect = require("expect.js"),
pc         = require("../..");

describe(__filename + "#", function () {

  it("wraps the context as a bindable object", function () {
    var a;
  });

  it("copies the values from the context to the passed in context", function () {

  });
});